# Lemur Facts

See **[AGENTS.md](./AGENTS.md)** for project rules, stack map, and the Ruflo/Grok swarm roster.

Short version: `init_db.py` is SoT → `build.py` regenerates `docs/` → Grok `spawn_subagent` + Ruflo MCP for multi-agent work (namespace `lemurs`).
